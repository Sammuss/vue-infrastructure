{
  "tasks": [
    {
      "command": "npx",
      "args": ["prettier", "--ignore-unknown", "--write", "${staged}"],
      "pathMode": "absolute"
    },
    {
      "command": "npx",
      "args": ["eslint", "--fix", "--ext", ".js,.vue", "src"],
      "pathMode": "absolute"
    }
  ]
}
