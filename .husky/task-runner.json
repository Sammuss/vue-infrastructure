{
  "tasks": [
    {
      "command": "npx",
      "args": ["prettier", "--ignore-unknown", "--write", "${staged}"],
      "pathMode": "absolute"
    },
    {
      "name": "eslint",
      "pathMode": "absolute",
      "command": "npm",
      "args": ["run", "eslint", "${staged}"],
      "include": ["**/*.ts", "**/*.vue", "**/*.js"]
    }
  ]
}
